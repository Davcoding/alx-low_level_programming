#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
void positivi_negative(int i);
int Largest_number(int a, int b, intc);
void print_remaining_days(int month. int day, int year);
int convert_day(int month, int day);

#endif
